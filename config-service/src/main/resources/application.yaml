server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/Calapzu/Curso-Microservicios-Spring-Boot
          search-paths: config-data
  aplication:
    name: config-service